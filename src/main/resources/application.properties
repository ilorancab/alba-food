spring.application.name=alba-food
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:albafood}
spring.datasource.username=${DB_USER:nacho}
spring.datasource.password=${DB_PASSWORD:password}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=false

# Flyway Migration Configuration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

# Application Configuration
app.baby-name=${BABY_NAME:Alba}

# Food tokenizer configuration
# app.food.stop-words=con,y,e,de,en,sin,la,el,los,las,un,una,al,del,su
# app.food.prefixes=pure de ,puré de ,crema de ,tritura de ,papilla de ,triturado de
